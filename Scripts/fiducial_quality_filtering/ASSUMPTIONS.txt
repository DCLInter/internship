ASSUMPTIONS MADE DURING THE REFACTOR
=====================================
(2026-08-16, while working without the project owner present)

BUG FOUND AND FIXED DURING VALIDATION (not an assumption, but flagging it
prominently since it changed actual output values):

checker.py's score_and_report() originally computed the criterion-4 combined
score with pandas .mean(axis=0), which divides by the count of NON-NaN
windows. The original metrics_functions.py/checker.py instead does
`sc.sum()/n` where n is the TOTAL window count (sc.shape[0]) - i.e. NaN
windows are treated as contributing 0 to the sum but still count in the
denominator, which is a systematically lower (stricter) score for any
signal with missing/NaN fiducial windows than a plain mean would give.
This was caught by validate_against_clean_subset.py: on the first 1500
signals of VitalDB_Train_Subset, 6 signals were kept by the refactor that
the original pipeline had discarded, all due to this. Fixed in checker.py's
score_and_report() to replicate sum(skipna=True)/n_windows exactly (see the
comment there). Re-verified 0 mismatches over the same 1500-signal slice
after the fix.

1. SCOPE: "fiducial quality pipeline" = the checking/scoring/discarding
   stage only (old checker.py + cleaning.py + metrics_functions.py +
   new_checker.py). It does NOT include pyPPG fiducial extraction itself
   (Scripts/new_extraction_fiducials.py), which is a separate upstream
   step this folder treats as already done. Confirmed pyPPG is not even
   installed on this machine, which is consistent with this stage not
   needing it.

2. DATA FORMAT TARGETED: the flat PulseDB-supplement format (one big
   dataset per subset, not per-patient groups), matching how
   Scripts/new_checker.py was actually being used (hardcoded filenames
   like Fiducial_Points_VitalDB_CalFree_Test_Subset.h5). The patient-
   grouped H5 format (p000001, p000002, ... each with its own
   "segments"/"Features" datasets) that Scripts/main.py's version of the
   pipeline targets was NOT covered - main.py was already flagged as a
   broken/stale prototype (mismatched Checker constructor, bad import) in
   an earlier conversation, so it wasn't used as a reference.

3. FIDUCIAL ATTRS: the original new_checker.py read fiducial-point column
   names from an HDF5 attribute (`group[dst].attrs.items()`), but the
   Fiducial_Points_VitalDB_Train_Subset.h5 file actually available on this
   machine has NO such attribute (verified empty). The refactor uses the
   hardcoded FIDUCIAL_ORDER list (16 names, matches Checker.fiducial_order
   in the original code) instead of reading it from the file. If a
   subset's fiducial file ever uses a different point ordering, this
   would silently mislabel columns - worth a sanity check if new data
   sources are added.

4. DROPPED THE UNUSED HDF5-READING BRANCH IN Cleaner.clean(): the original
   cleaning.py supported two ways of calling .clean() - passing an
   in-memory `data_ext` dict, or passing a path to read + clean directly
   from HDF5. Only the data_ext path was ever used by new_checker.py, and
   the HDF5-reading path had a real bug (self.demo_info / self.attributes
   were left empty when data_ext was used instead, so csvReport() would
   KeyError on demographic columns if called after a data_ext clean()).
   The refactor keeps only the data_ext path and removes the demographic
   columns from csv_report() entirely (see point 5) rather than fixing a
   code path nothing currently calls.

5. csv_report() no longer includes age/gender/weight/height/BMI columns
   that the original Cleaner.csvReport() had. Those required demographic
   attributes that were only populated by the removed HDF5-reading branch
   (see point 4). Demographics-vs-removal reporting is still produced, but
   via Demographic_Info_<subset>.xlsx in run_pipeline.py (which reads
   demographics straight from the Features_<subset>.h5 file), not via
   cleaner.csv_report(). If you need both in one file, they can be merged.

6. VALIDATION SUBSET: validate_against_clean_subset.py defaults to the
   Train subset because, at the time of writing, the CalFree_Test subset
   was MISSING its Fiducial_Points_VitalDB_CalFree_Test_Subset.h5 file in
   the SupplementarySubsets folder (Features file was present, fiducials
   file was not) - this was already flagged earlier in this conversation.
   If that file turns up, the same script works for CalFree_Test by
   changing SUBSET_NAME at the top.

7. VALIDATION METHOD: compares the refactored pipeline's output against
   the pre-existing Clean_Features_VitalDB_Train_Subset_90.h5 by exact
   array match (PPG_Features values, Subject order, SBP/MAP/Age), not by
   re-deriving expected removal counts from first principles. A pass means
   "this refactor reproduces the old pipeline's output exactly for this
   file", not "the quality criteria themselves are correct" - that
   judgment call (what thresholds/weights to use) is the project owner's,
   and is exactly what the alpha/beta/threshold sweep this refactor is
   meant to support will help explore.

8. ENVIRONMENT: .venv310/ did not exist in the repo on this machine at the
   start of this session. It was created fresh from the raw Python 3.10
   interpreter found at
   C:\Users\Felipe Saldarriaga\AppData\Local\Programs\Python\Python310\python.exe
   and Scripts/bp_lgbm/Requirements.txt was pip-installed into it in full
   (including NPEET from GitHub), per your instruction to rebuild it. This
   venv is untracked/gitignored as documented in CLAUDE.md - freeze/commit
   requirements as you see fit; nothing here assumes a specific frozen
   version beyond what Requirements.txt already pins (nothing is pinned -
   Requirements.txt has no version numbers, so exact package versions
   installed are whatever pip resolved as latest-compatible on 2026-08-16).

9. OUTPUT LOCATION: run_pipeline.py writes everything to
   fiducial_quality_filtering/outputs/ by default (created if missing),
   rather than back into the SupplementarySubsets OneDrive folder the
   original script wrote into. This keeps generated artifacts out of the
   OneDrive-synced data folder and out of git (outputs/ should be added to
   .gitignore if not already covered by the existing CV_results*/mi_
   results/etc. patterns - it currently is NOT explicitly ignored, please
   check before committing anything from there).


ASSUMPTIONS FOR THE ALPHA/BETA + THRESHOLD SWEEP
(2026-08-17, build_cache.py / sweep_alpha_beta.py / sweep_thresholds.py)
==========================================================================

10. RAW METRICS CACHE FORMAT/PRECISION: raw_metrics_cache.py stores
    alignment/consistency as float32 (not float64) to keep the cache size
    manageable (~1.1GB for the Train subset's 465,480 signals - two
    (n_signals, max_windows, 16) arrays). This introduces score
    differences up to ~1e-5 vs the float64 per-signal path in checker.py -
    verified negligible (never changes a report/keep decision) by
    validating against checker.py's QualityChecker on 5,000 signals across
    3 different alpha/beta/threshold combinations before trusting it for
    the real sweep.

11. WINDOW COUNT DIFFERS BY SUBSET: Train's fiducial file has 336 rows
    (21 windows/signal before dropna); the newly-uploaded CalFree_Test
    file has 320 rows (20 windows/signal) - a different fixed shape per
    subset, not a bug. raw_metrics_cache.py derives max_windows from each
    file's own shape rather than hardcoding it, so this is handled
    automatically, but it does mean the two subsets' cache files are not
    interchangeable/comparable at the array-shape level (this doesn't
    matter for anything the sweep scripts do, since scoring is always
    per-subset).

12. SUBJECT-DROP DEFINITION: reported BOTH interpretations side by side in
    the sweep summary CSVs, per your answer - "subjects with >=1 dropped
    sample" (n_subjects_any_dropped) and "subjects entirely dropped, 100%
    of their samples gone" (n_subjects_fully_dropped). Note the first one
    is 100% for essentially the whole alpha/beta/threshold grid tested
    (every subject has at least one bad signal somewhere) - it's the
    "fully dropped" number that actually moves and is probably the more
    decision-relevant one, but both are in the CSVs.

13. GRID CHOICES (edit at the top of each sweep script to change):
    - alpha (w_consistency): 0.0 to 1.0 in steps of 0.1 (11 points), with
      beta = 1 - alpha, thresholds fixed at 90/90 (the pipeline default).
    - thres_fiducials = thres_score together: 70 to 95 in steps of 5 (6
      points), alpha/beta fixed at 0.25/0.75 (the pipeline default). Note
      the observed jump in the Train subset between thres=85 (15.2%
      dropped) and thres=90 (59.5% dropped) is large and worth a closer
      look with a finer grid (e.g. step of 1) around 85-90 if you want to
      pick a threshold precisely rather than just from {70,75,...,95}.
    Neither script sweeps a genuine 2D grid (alpha x beta x threshold
    together) - see README.txt's "WRITING YOUR OWN SWEEP" section for how
    to build one on top of the same cache without recomputing anything.

14. LOCAL SAMPLE INDEX, NOT GLOBAL: the dropped-signals JSON indexes each
    dropped sample by its position WITHIN its own subject's block of
    signals in Features_<subset>.h5 (0, 1, 2, ... per subject), not by a
    global 0..465479 index. This was chosen because it's what you'd
    actually filter by if loading data grouped/indexed by subject at
    training time; if you need the global index instead it's trivial to
    derive (cumulative subject signal counts + local index), just flag it
    if that's what you actually want.

15. OUTPUT LOCATION FOR THE SWEEP RESULTS: written to the
    Filtering_Sensitivity folder you created, one subfolder per subset
    (matching the subset names used throughout: VitalDB_Train_Subset,
    VitalDB_CalFree_Test_Subset), NOT into
    fiducial_quality_filtering/outputs/ - these are meant to be reused
    from outside this repo (at training time), so they went to the shared
    OneDrive location you specified rather than the gitignored local
    outputs/ folder.


CLEAN_FEATURES REBUILD + DISTRIBUTION SHIFT UNIFICATION
(2026-08-18, rebuild_clean_features.py / distribution_shift.py)
==========================================================================

16. bmin/bmax CORRECTION, DIRECTION CLARIFIED: earlier in this project
    bmin=40/bmax=200 was flagged as a possibly-accidental change from
    bmin=50/bmax=180. The project owner has since clarified the opposite:
    40/200 is CORRECT and current; the Clean_Features_*_{80,90}.h5 files
    that existed on disk before this session were built with the WRONG
    50/180 range and needed replacing. rebuild_clean_features.py
    reconstructed all 4 files (both subsets x both thresholds) directly
    from the existing (already-correct, already built from the 40/200
    cache) sweep_thresholds.py JSONs - no cache rebuild or re-sweep was
    needed, since those were already right. Verified: (a) reconstructed
    keep-counts match threshold_sweep_summary.csv exactly for all 4
    files, (b) a live QualityChecker re-run on the first 2,000 Train
    signals matches the JSON-reconstructed keep/drop decisions bit-for-
    bit.

17. DISTRIBUTION-SHIFT SCOPE, PER PROJECT OWNER (revised after initial
    misread - see commit history): unified distribution_shift.py keeps
    ONLY the pooled (whole-column, all-samples) granularity from the old
    comparator.py - the per-patient and per-patient-median granularities
    were dropped. Everything ELSE comparator.py computed at pooled
    granularity IS kept/ported: %-change of mean/median/Q1/Q3/IQR/MAD, KS
    test (statistic D + p-value), and IQR/MAD outlier detection + Jaccard
    overlap of the outlier sets. If you want the per-patient granularities
    back, comparator.py's logic is the reference (see its byPatient/
    byFeature_medians methods) - nothing was deleted from comparator.py
    itself.

18. DEMOGRAPHIC/LABEL COLUMNS NOW GET REAL SHIFT METRICS: Age, BMI,
    Height, Weight, SBP, DBP, MAP are analyzed with the exact same
    normalized-Wasserstein + KS treatment as the 28 PPG features - this
    closes the gap where Dataset_exploration.py only had describe()-based
    summaries for these. Gender (categorical) and SF (constant) are
    loaded but deliberately excluded from shift stats.

19. IMPUTATION BEFORE SHIFT: both the original and the clean dataframe are
    patient-wise median-imputed (bp_lgbm.preprocessing.median_impute_
    patientwise) before computing Wasserstein/KS, fixing the old script's
    asymmetry (it only imputed the original side). Emits a harmless
    pandas FutureWarning (DataFrameGroupBy.apply on grouping columns) from
    the pre-existing preprocessing.py - not something introduced here, not
    fixed as part of this task.

20. CROSS-PACKAGE IMPORT: distribution_shift.py imports from
    Scripts/bp_lgbm/ via a sys.path insert at the top of the file. This
    is explicitly a temporary shortcut - the project owner's plan is to
    fix cross-package imports and relative paths across the whole repo in
    one pass before publication, not per-script now.

21. OLD SCRIPTS LEFT IN PLACE: comparator.py, comparator_example.py,
    new_comparator.py, and Dataset_exploration.py are untouched -
    superseded but not deleted/fixed (Dataset_exploration.py's
    ImportError on the missing DATASET_EXPLORATION_RESULTS in
    local_paths.py was NOT fixed, since that script is no longer the
    path forward).

22. PRE-FIX OUTPUTS ARCHIVED: every file that was already sitting in the
    OneDrive Distribution_Analysis folder (built under the wrong 50-180
    HR range) was moved into a legacy_50-180_bpm/ subfolder there, not
    deleted, per the project owner's instruction - 12 files
    (Agreggated_Results_Distrib_Change_{80,90}.xlsx, Demographic_Info_
    VitalDB_Train_Subset_{80,90}.xlsx, outliers_change_{80,90}.xlsx,
    overlap_JaccardSimilarity_{80,90}.xlsx +
    overlap_JaccardSimilarity_Full_Set_Features_90.xlsx, stats_change_
    {80,90}.xlsx + stats_change_90_Full_Set_Features.xlsx).
